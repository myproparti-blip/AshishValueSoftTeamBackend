services:
  - type: web
    name: server
    runtime: node
    rootDir: .
    buildCommand: npm install
    startCommand: npm run prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5001
